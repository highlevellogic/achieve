// Little JavaScript framework that can be fetched from another origin (different server)
// This is part of CORS testing.
// When the CORS policy in the test is ('*', 'public')

function test () {
	return "hello from test in cors/public/public/frameworks2/littleframework.js.";
}
